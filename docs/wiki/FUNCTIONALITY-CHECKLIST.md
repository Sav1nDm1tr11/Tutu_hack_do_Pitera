# Проверка заявленного функционала — 19.08.2026

Легенда: ✅ проверено запуском · 🟡 есть в коде, вживую не проверялось · ❌ сломано/отсутствует

## Поиск поездки

| Пункт | Статус | Комментарий |
|---|---|---|
| Форма: откуда/куда, one-way/round-trip, даты, взрослые/дети, бюджет | 🟡 | `features/search/SearchForm.tsx`, схема `search-form-schema.ts`, валидация общая с сервером (zod) |
| Подсказки городов из демо-каталога, id а не свободный текст | 🟡 | `PlacePicker.tsx` + `GET /api/places`. **Ограничение: в каталоге ~8 городов** (Москва, СПб, Екб, Казань, Новосибирск, Сочи, Н.Новгород…). При живом MCP это лишнее самоограничение: сервер Туту сам резолвит «Питер», «Мск» и любой топоним |
| Ограничения: без ночных, ≤1 пересадка, запас на пересадку, прибыть не позже | 🟡 | `ConstraintChips.tsx` → `maxTransfers:1`, `minimumTransferPolicy:'extra'`, `arriveBeforeLocalTime` |
| Приоритеты: сбалансированный / дешевле / комфортнее | 🟡 | `preset: 'balanced'\|'price'\|'comfort'` → веса preferences |
| Прогресс по фазам | ✅ | 6 событий `plan.progress` приходят в потоке (после починки P0-1) |

## План маршрута

| Пункт | Статус | Комментарий |
|---|---|---|
| До трёх конфигураций | ❌→ | в live-режиме `configurations: 0` (все варианты в карантине, см. REVIEW P0-3) |
| Сводка: цена, длительность, оценки, уверенность | 🟡 | `PlanSummary.tsx`, домен считает confidence по доступным сигналам |
| Карточки этапов: транспорт / отель / ожидание | 🟡 | `StageCard.tsx` (420 строк — самый крупный компонент) |
| Замена варианта в нижней шторке | 🟡 | `OptionListSheet.tsx` + `PATCH`-ручка выбора с проверкой revision |
| Предупреждения: демо-данные, частичный инвентарь, недоступная модель | ✅ | приходят в `plan.warnings`, формат подтверждён живым прогоном |
| Метки свежести цены | 🟡 | `FreshnessBadge.tsx`, `domain/time/freshness.ts` |

## План Б и риски

| Пункт | Статус | Комментарий |
|---|---|---|
| Жёсткие фильтры (бюджет, ночь, пересадки, дедлайн) | 🟡 | `domain/scoring/hard-filters.ts`, покрыто тестами |
| Сигналы риска | 🟡 | `domain/scoring/risk-signals.ts` |
| Ограниченный план Б (≤2 этапа, ≤2 опции) | 🟡 | `domain/fallback/build-fallback.ts` + бюджеты из `.env` |
| Честный статус «недоступно» с причиной | 🟡 | `FallbackPlan.status` |

## Карта

| Пункт | Статус | Комментарий |
|---|---|---|
| 3D-глобус MapLibre без внешних тайлов | 🟡 | `RouteGlobe.tsx` + `world-atlas` локально |
| Кадрирование маршрута / этапа | 🟡 | `domain/geo/viewport.ts`, покрыто тестами |
| Текстовая схема без WebGL / reduced-motion | 🟡 | `RouteScheme.tsx`, `use-prefers-reduced-motion.ts` |
| **Вес бандла** | ⚠️ | `maplibre` чанк 931 кБ (242 кБ gzip), всего precache 1.55 МБ — тяжело для мобильной PWA на демо |

## Оформление

| Пункт | Статус | Комментарий |
|---|---|---|
| Кнопка ведёт на allowlisted URL tutu.ru | ✅ | `domain/checkout/allowlist.ts`: https-only, 9 хостов Туту, отсекает креды в URL. Реальные `checkout_url` от MCP (`www.tutu.ru`) проходят |
| Приложение не бронирует и не принимает оплату | ✅ | booking-tool'ы в deny-list, подтверждено: `create_checkout_link` отклонён discovery |
| Офлайн/устаревшие данные отключают оформление | 🟡 | по коду да, вживую не проверялось |

## Офлайн и PWA

| Пункт | Статус | Комментарий |
|---|---|---|
| manifest + service worker | ✅ | сборка генерирует `sw.js`, `manifest.webmanifest`, 17 записей precache |
| Последний план в IndexedDB | 🟡 | `lib/plan-storage.ts` (idb) |
| Экраны offline / about-data / 404 | 🟡 | маршруты объявлены в `main.tsx` |
| Badge «Демо-данные» / офлайн | 🟡 | зависит от `inventorySource`; **сейчас всегда «демо», т.к. live не доезжает** |

## Сервер и движок

| Пункт | Статус | Комментарий |
|---|---|---|
| NDJSON-стрим `POST /api/plan` | ❌ | **в `main` отдаёт 0 байт** (P0-1). После правки — 8 событий |
| Получение плана и замена варианта с проверкой revision | 🟡 | `selection-service.ts` |
| Health, снимок возможностей, подсказки мест | ✅ | `/api/health` отвечает; `/api/capabilities`, `/api/places` есть в коде |
| Детерминированный scoring в домене | ✅ | 184 теста, домен без сети и React; веса и confidence считает код, не модель |
| LLM через OpenRouter, без ключа — шаблоны | 🟡 | `deterministic-planner` работает (`plannerMode: "deterministic"` в логе). С ключом не проверялось |
| Инвентарь: fixtures по умолчанию, live написан | ⚠️ | live **достижим**, но даёт 0 вариантов (P0-3…P0-5) |
| Лимит запросов, TTL, Docker | 🟡 | rate-limit и TTL-репозиторий есть; Dockerfile есть, сборка не проверялась |

## Что заявлено как «сознательно не работает»

- «Реальные билеты и цены с mcp.tutu.ru (хост не отвечает)» — **утверждение неверно**:
  хост отвечает, инструменты и цены доступны. Это не ограничение, а баг интеграции.
- Аккаунты, БД, оплата — согласен, вне scope и правильно, что их нет.

## Верификация baseline (чистый `main`, коммит bfe7e8a)

```
pnpm test        1 failed | 183 passed (184)   ← tool-catalog.test.ts
pnpm lint        2 errors                       ← auto-gateway.ts: consistent-type-imports
pnpm typecheck   OK
pnpm build       OK (PWA: 17 precache entries, 1550 KiB)
pnpm verify      ПАДАЕТ (из-за lint и теста)
```
